const data = require('../data/zoo_data');

function getEmployeesCoverage(objeto) {
  // const getEmployee = employees.find((employee) => {
  //   objeto === employee.firstName || employee.lastName || employee.id

  // })

//   const employeeInfo = employees.map((employee) => {
//     return {
//       id: employee.id,
//       fullName: `${employee.firstName} ${employee.lastName}`,
//       species: employee.responsibleFor,
//       locations: species.filter((specie) => employee.responsibleFor.includes(specie.id)),
//     }
//   })
// return employeeInfo
}
// console.log(getEmployeesCoverage({ name: 'Sharonda' }));

// function getEmployees(name) {

// }
// function getSpecies(objeto) {
//   const getEmployees = employees.filter((employee) => employee
//     .lastName === Object.values(objeto) || employee
//     .firstName === Object.values(objeto) || employee.id === Object.values(objeto) );
//   const getSpeciesResponsable = getEmployees.map((getEmployee) => getEmployee.responsibleFor);

//   return getSpeciesResponsable;
// }

//   function getLocation(location) {

//   }

// console.log(getSpecies({ name: 'Sharonda' }));
module.exports = getEmployeesCoverage;
